[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ml_from_scratch"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy"]

[tool.hatch.build.targets.wheel]
packages = ["src/ml_from_scratch]
